@model GEA.AvaliacaoTecnica.Application.ViewModels.VeiculoViewModel

@{
    ViewBag.Title = "Cadastrar";
}

@using (Html.BeginForm("Registrar", "EntradaVeiculo", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    <div class="row">
        <div class="col-lg-3"></div>
        <div class="col-lg-6">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    Registrar Entrada Veículo
                </div>
                <div class="panel-body">
                    <hr />
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        @Html.LabelFor(model => model.Placa, htmlAttributes: new { @class = "control-label col-md-4" })
                        <div class="col-md-8">
                            @Html.TextBoxFor(model => model.Placa, new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.Placa, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    @if (ViewBag.Error != null)
                    {
                    <div class="alert alert-danger">
                        <a href="@Url.Action("Cadastrar", "Veiculo")" class="alert-link">
                            @ViewBag.ErrorMensager
                        </a>
                    </div>
                    }

                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Registrar" class="btn btn-primary" />
                        </div>
                    </div>
                </div>
                <div class="panel-heading">
                </div>
            </div>
        </div>
        <div class="col-lg-3"></div>

    </div>






}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
