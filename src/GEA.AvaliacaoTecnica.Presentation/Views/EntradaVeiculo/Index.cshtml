@model IEnumerable<GEA.AvaliacaoTecnica.Application.ViewModels.RegistroEntradaViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Registro de Entrada Veículos</h2>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Dados
            </div>
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <a href="@Url.Action("Registrar", "EntradaVeiculo")">
                        <button type="button" class="btn btn-primary" style="margin-bottom: 15px;"> <i class="fa fa-paper-plane"></i> Adicionar</button>
                    </a>
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Hora Entrada</th>
                                <th>Código</th>
                                <th>Placa Veículo</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="gradeA">
                                    <td>@Html.DisplayFor(modelItem => item.HoraEntrada)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Codigo)</td>
                                    <td>@Html.DisplayFor(modelItem => item.Veiculo.Placa)</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
